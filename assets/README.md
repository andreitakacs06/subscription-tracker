# Assets

Place the following files here before publishing. Expo will use them automatically:

- `icon.png` — 1024×1024 app icon
- `adaptive-icon.png` — 1024×1024 Android adaptive foreground
- `splash.png` — splash screen image
- `favicon.png` — web favicon

For development with `npx expo start`, Expo provides defaults if these are missing.
